# Cloud Shell configuration file
# This file defines the Cloud Shell environment for this repository

cloudshell:
  # Open these files in the editor on startup
  open_in_editor:
    - README.md
  
  # Custom startup script that runs when repo is opened
  startup_script: |
    #!/bin/bash
    
    # Make scripts executable
    chmod +x setup.sh init-git.sh
    
    # Display welcome banner
    echo ""
    echo "═══════════════════════════════════════════════════════════════"
    echo "  🚀 Cloud Run Deployment Environment"
    echo "═══════════════════════════════════════════════════════════════"
    echo ""
    echo "✅ Repository cloned successfully!"
    echo "✅ Scripts are now executable"
    echo ""
    echo "📚 Quick Start Options:"
    echo ""
    echo "  1. 🎓 Interactive Tutorial (Recommended):"
    echo "     cloudshell launch-tutorial tutorial.md"
    echo ""
    echo "  2. 🖥️  Beautiful CLI Setup:"
    echo "     ./setup.sh"
    echo ""
    echo "  3. ⚡ Manual Terraform:"
    echo "     cp example.tfvars terraform.tfvars"
    echo "     nano terraform.tfvars"
    echo "     terraform init && terraform apply"
    echo ""
    echo "═══════════════════════════════════════════════════════════════"
    echo ""

