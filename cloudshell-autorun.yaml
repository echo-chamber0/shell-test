# Cloud Shell configuration file - AUTO-RUN VERSION
# This version automatically starts setup.sh without prompting
# 
# To use this version:
# 1. Rename this file to cloudshell.yaml
# 2. Commit and push
# 3. Use the same Cloud Shell link

cloudshell:
  # Open these files in the editor on startup
  open_in_editor:
    - README.md
  
  # Custom startup script that auto-runs setup.sh
  startup_script: |
    #!/bin/bash
    
    # Make scripts executable
    chmod +x setup.sh init-git.sh
    
    # Display welcome banner
    echo ""
    echo "═══════════════════════════════════════════════════════════════"
    echo "  🚀 Cloud Run Deployment - AUTO-SETUP MODE"
    echo "═══════════════════════════════════════════════════════════════"
    echo ""
    echo "✅ Repository cloned successfully!"
    echo "✅ Starting interactive deployment in 3 seconds..."
    echo ""
    echo "💡 TIP: Press Ctrl+C to cancel and run manually"
    echo ""
    
    # Give user a moment to cancel if needed
    sleep 3
    
    # Auto-run the setup script
    ./setup.sh

